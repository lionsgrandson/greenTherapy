name: Final language cleanup
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/final-language-cleanup.yml'
permissions:
  contents: write
jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install beautifulsoup4
      - run: python scripts/final_language_fix.py
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm -f .github/workflows/final-language-cleanup.yml
          git add -A
          git commit -m "Clean final English copy and accessibility links"
          git push origin HEAD:main
